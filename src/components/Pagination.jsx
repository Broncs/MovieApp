import React from "react";

const Pagination = () => {
  return (
    <section>
      <div>
        <button>anterior</button>
        <button>proximo</button>
      </div>
    </section>
  );
};

export default Pagination;
